import { assert, expect, should } from 'chai';
import {} from './${JS_Filename}'

function setup() {
    
}

describe('${Test_description}', () => {
    it('Case 1', () => {
        
    });
}); 