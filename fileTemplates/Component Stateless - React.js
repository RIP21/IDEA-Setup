import React, {PropTypes, Component} from 'react';

const $NAME = ({...props}) => {
    return (
    <div>
        //Content go there
    </div>
    );
};

$NAME .propTypes = {
    //field: PropTypes.object,
};

export default $NAME; 